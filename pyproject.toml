[project]
name = "trading-system"
version = "0.1.0"
description = ""
authors = [
    {name = "burak.kadirbeyoglu",email = "k.burak@wako3d.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
]

[tool.poetry]
packages = [{include = "trading_system", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
